// Allows to determine if an element is within an array
let numbers = [1, 3, 4, 7, 19, 22];
console.log(numbers.includes(7));

const list = ["David", "Oskar", "Anne"];
console.log(list.includes("Oskar"));
